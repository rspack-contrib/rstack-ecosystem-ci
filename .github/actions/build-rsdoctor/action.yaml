name: 'Build Rsdoctor'
description: 'Checkout and install rsdoctor'
inputs:
  repository:
    description: 'The rsdoctor repository to use'
    required: true
    default: 'web-infra-dev/rsdoctor'
  path:
    description: 'Destination path to the rsdoctor repository'
    required: true
    default: 'workspace/rsdoctor'
  ref:
    description: 'The branch, tag or SHA to checkout'
    required: true
    default: 'main'
runs:
  using: composite
  steps:
    - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
      with:
        node-version: 22
    - shell: bash
      name: Install package manager
      run: |
        npm install -g corepack@latest --force
        echo "Corepack version: $(corepack --version)"
        corepack enable

    - name: Checkout Rsdoctor repo
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      with:
        repository: ${{ inputs.repository }}
        path: ${{ inputs.path }}
        ref: ${{ inputs.ref }}
    - name: Install dependencies
      shell: bash
      run: |
        cd ${{ inputs.path }}
        pnpm install
